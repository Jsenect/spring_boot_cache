spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.137.240:5432/cache_demo
    password: postgres
    username: postgres

  devtools:
    restart:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update  #
    show-sql: true